# DivScout Environment Configuration
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file to version control!

# ============================================================================
# Database Configuration (Required)
# ============================================================================
DB_HOST=localhost
DB_PORT=5432
DB_NAME=edgar_dividends
DB_USER=your_database_user
DB_PASSWORD=your_secure_password_here
DB_SSLMODE=require

# ============================================================================
# SEC API Configuration (Required)
# ============================================================================
# IMPORTANT: You MUST set this to your real name and email address.
# The SEC requires proper identification per their Fair Access policy.
# Example: "John Doe john.doe@example.com"
# See: https://www.sec.gov/os/accessing-edgar-data
SEC_USER_AGENT=YourName your.email@domain.com

# ============================================================================
# Data Collection Configuration (Optional)
# ============================================================================
# Default year range for data collection (XBRL fetches all available data regardless)
START_YEAR=2020
END_YEAR=2025

# Download directories for bulk data (optional features)
DOWNLOAD_DIR=./data/downloads
EXTRACT_DIR=./data/extracted
